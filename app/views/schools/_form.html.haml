= simple_form_for @school, html: { class: 'custom' } do |form|
  = form.error_notification

  .form-inputs
    = form.input :name
    = form.association :teachers, collection: Teacher.all

  .form-actions
    = form.button :submit
