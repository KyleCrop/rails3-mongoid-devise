= simple_form_for @student, html: { class: 'custom' } do |form|
  = form.error_notification

  .form-inputs
    = form.input :name
    = form.association :classroom, collection: Classroom.all

  .form-actions
    = form.button :submit
